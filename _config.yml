url: "https://cs4sg.github.io/" # the base hostname & protocol for your site, e.g. http://example.com
baseurl: "" # the subpath of your site, e.g. /blog
title: "" # the name of your site, e.g. ACME Corp.
collections:
    research_projects:
        output: true
    publications:
        output: true
    funding:
        output: true
    people:
        output: true
    talks:
        output: true
    code:
        output: true
defaults:
  - scope:
      path: "assets/resources"
    values:
      image: true
